package Vektorrechnung;

import oop.IO;

public class Vektoren {
	
	public static int[] vektorAddition (int[] v1, int[] v2) {
		//addition
		// return ergebnisvektors
	}
	
	

}

// =============================================================================
class menue( ) {
	if () {
		System.out.println("ersten Vektor anlegen - wie viele Einträge: ");
		int anzahlElemente = IO.readInteger();
		int[] vektor1 = IO.IntArray(anzahlElemente);
		// ...
		int[] ergebnisvektor = Vektoren.vektorAddition(vektor1, v2);
		
	}
}
